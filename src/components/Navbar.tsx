const Navbar = () => {
  return (
    <nav className='w-full fixed left-0 top-0 z-40 md:py-8 bg-gradient  flex justify-end'>
      <div className='hidden md:flex items-center w-max mr-10'></div>
    </nav>
  );
};

export default Navbar;
